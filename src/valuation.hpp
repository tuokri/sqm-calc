/** \file valuation.hpp
 *  \brief Valuation header. Valuation is a <char, int> map used when evaluating
 *         TElement<T> or ElementarySquareMatrix<T> to concrete values.
 */

#ifndef VALUATION_H
#define VALUATION_H

#include <map>

using Valuation = std::map<char, int>;

#endif // VALUATION_H
